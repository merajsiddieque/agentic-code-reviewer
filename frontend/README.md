# Frontend (React)

This directory is reserved for the React frontend application.

### Planned Tech Stack:
- React (Vite / Next.js)
- Monaco Editor (Code diff and syntax highlighting)
- TailwindCSS / Modern Design System
- Lucide Icons & Radix UI

To initialize the React app later, run:
```bash
npm create vite@latest . -- --template react-ts
```
